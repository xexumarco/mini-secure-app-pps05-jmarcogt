const express = require('express');
const helmet = require('helmet'); // Importas helmet

const app = express();

// 1º Aplicamos las protecciones de seguridad
app.use(helmet()); 

// 2º Luego ya definimos las rutas de la web (todo lo que sea app.use o app.get)
app.use('/', indexRouter); 
// ...const helmet = require('helmet');
app.use(helmet());
